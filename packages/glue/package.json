{
  "name": "@websummit/glue",
  "version": "1.0.0",
  "private": true,
  "main": "src/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "build:standalone": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json --noEmit",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "dependencies": {
    "@websummit/graphql": "*",
    "qs": "*",
    "react": "*",
    "react-router-dom": "*",
    "single-spa": "*",
    "single-spa-layout": "*"
  },
  "devDependencies": {},
  "files": [
    "src"
  ],
  "types": "src/index",
  "license": "MIT"
}
